language: node_js
env:
  - DB_TEST_DATABASE_URL=postgres://damola:damola@localhost:5432/travis SECRET_KEY=TEST
node_js:
  - "stable"
before_install:
  - npm install -g npm@latest
cache:
  directories:
    - "node_modules"

before_script:
  - psql -c "CREATE USER damola WITH PASSWORD 'damola';" -U postgres
  - psql -U postgres < server/db/indexTest.sql
after_success:
  -  npm run coverage
services:
  - postgresql
